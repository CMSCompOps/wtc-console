[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=wtc-console
Group=nginx
WorkingDirectory=/opt/wtc-console
ExecStart=/opt/wtc-console/wtc-console-env/bin/gunicorn --workers 2 --bind unix:/opt/wtc-console/wtc-console.sock djangoreactredux.wsgi:application

[Install]
WantedBy=multi-user.target
